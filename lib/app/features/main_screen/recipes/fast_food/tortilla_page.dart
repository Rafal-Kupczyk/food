import 'package:flutter/material.dart';

class TortillaPage extends StatelessWidget {
  const TortillaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
